extends layout
block content 
    h1= title 
    if book_list.length 
        ul 
            each book in book_list 
                li
                    a(href=book.url) #{book.title}
                    | (#{book.author.name})
                    // use the pipe so that the author name is not a hyper link
    else 
        p There are no books.